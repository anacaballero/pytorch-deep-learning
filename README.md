# pytorch-deep-learning
PyTorch implementations: weight initialization strategies, batch normalization, transfer learning (VGG16/ResNet50), CNN feature analysis, optimization benchmarks, and custom dataset pipelines
